const $=s=>document.querySelector(s),name=new URLSearchParams(location.search).get('name');async function getWeek(n){try{const r=await fetch(`data/2026/week-${String(n).padStart(2,'0')}.json`);return r.ok?await r.json():null}catch{return null}}const pct=n=>`${Math.round(n)}%`;async function load(){if(!name)throw Error('No player selected');$('#name').textContent=name;document.title=`${name} — NextWeekGPT`;const [w1,w2,registry]=await Promise.all([getWeek(1),getWeek(2),fetch('data/players.json').then(r=>r.json()).catch(()=>({players:[]}))]),weeks=[w1,w2].filter(Boolean),finals=weeks.filter(w=>w.status==='final');let picks=0,wins=0,dogs=0,dogWins=0,prob=0,own=0,pts=0,weeklyWins=0;const history=[];finals.forEach(w=>{const p=(w.players||[]).find(x=>x.name.toLowerCase()===name.toLowerCase()||x.name.split(' ')[0].toLowerCase()===name.toLowerCase());if(!p)return;pts+=p.points||0;if(w.weeklyWinner===p.name)weeklyWins++;const detail=p.picks.map((pick,i)=>{const g=w.games[i],away=pick===g.away,pr=away?g.awayNoVigWinPct:g.homeNoVigWinPct,ow=away?g.awayPoolPct:g.homePoolPct,win=pick===g.winner;picks++;wins+=win?1:0;if(pr<50){dogs++;dogWins+=win?1:0}prob+=pr||0;own+=ow||0;return{pick,g,pr,ow,win}});history.push({w,p,detail})});$('#summary').innerHTML=[['Points',pts],['Accuracy',picks?pct(wins/picks*100):'—'],['Underdog rate',picks?pct(dogs/picks*100):'—'],['Dog record',`${dogWins}-${dogs-dogWins}`],['Avg implied',picks?pct(prob/picks):'—'],['Avg ownership',picks?pct(own/picks):'—'],['Weekly wins',weeklyWins],['Picks tracked',picks]].map(([l,v])=>`<div class="card"><div class="label">${l}</div><div class="value small-value">${v}</div></div>`).join('');const reg=registry.players.find(x=>x.name.toLowerCase()===name.toLowerCase()||x.name.split(' ')[0].toLowerCase()===name.toLowerCase());$('#notes').innerHTML=reg?.notes?.length?reg.notes.map(n=>`<div class="note profile-note">${n}</div>`).join(''):'<p class="muted">No manually confirmed tendencies yet. Let the data cook.</p>';$('#history').innerHTML=history.length?history.map(h=>`<section class="history-week"><h3><a href="week.html?season=2026&week=${h.w.week}">Week ${h.w.week}</a> — ${h.p.points} points • TB ${h.p.tiebreaker}</h3><div class="pick-chips">${h.detail.map(x=>`<span class="chip ${x.win?'correct':'wrong'}" title="${x.g.away} @ ${x.g.home}; market ${x.pr?.toFixed(1)}%; pool ${x.ow}%">${x.pick} <small>${x.pr?.toFixed(0)}% / ${x.ow}%</small></span>`).join('')}</div></section>`).join(''):'<p class="muted">No finalized picks stored for this player yet.</p>'}load().catch(e=>document.querySelector('main').innerHTML=`<section class="panel"><h2>${e.message}</h2><p><a href="index.html">Return to season</a></p></section>`);